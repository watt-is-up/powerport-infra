apiVersion: apps/v1
kind: Deployment
metadata:
  name: station-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: station-service
  template:
    metadata:
      labels:
        app: station-service
    spec:
      containers:
        - name: station-service
          image: ghcr.io/watt-is-up/station-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
---
apiVersion: v1
kind: Service
metadata:
  name: station-service
spec:
  selector:
    app: station-service
  ports:
    - port: 80
      targetPort: 8080
