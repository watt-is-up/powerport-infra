apiVersion: apps/v1
kind: Deployment
metadata:
  name: provider-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: provider-service
  template:
    metadata:
      labels:
        app: provider-service
    spec:
      containers:
        - name: provider-service
          image: ghcr.io/watt-is-up/provider-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
---
apiVersion: v1
kind: Service
metadata:
  name: provider-service
spec:
  selector:
    app: provider-service
  ports:
    - port: 80
      targetPort: 8080
